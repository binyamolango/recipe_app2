<%= form_with(model: [@recipe, recipe_food]) do |form| %>
  <% if recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_food.errors.count, "error") %> prohibited this recipe_food from being saved:</h2>

      <ul>
        <% recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>

    <div>
      <%= form.label :food_id, "Food", style: "display: block" %>
      <%= form.select :food_id, options_for_select(Food.all.map { |food| ["#{food.name} (in #{food.measurement_unit})", food.id] }) %>
    </div>

    <div>
      <%= form.label :quantity, style: "display: block" %>
      <%= form.number_field :quantity %>
    </div>

    <%= form.submit %>
  </div>
<% end %>
